<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.2</version>
	</parent>
	<groupId>com.alliebooks</groupId>
	<artifactId>alliebooks-v2</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>Alliebooks-v2</name>
	<description>Alliebooks Version 2</description>

	<properties>
		<java.version>21</java.version>
		<maven.compiler.release>21</maven.compiler.release>
		<jwt.version>0.12.6</jwt.version>
		<frontend-src-dir>${project.basedir}/src/main/frontend</frontend-src-dir>
		<node.version>v18.17.1</node.version>
		<frontend-maven-plugin.version>1.12.1</frontend-maven-plugin.version>
        <lombok.version>1.18.38</lombok.version>
		<!-- tess4j -->
		<tess4j.version>5.16.0</tess4j.version>
		<tess4j.patched.jar>${project.build.directory}/patched-deps/tess4j-${tess4j.version}.jar</tess4j.patched.jar>
		<tess4j.native.dir>${project.basedir}/src/main/resources/native/linux-x86-64</tess4j.native.dir>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
		</dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
        </dependency>
		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-core</artifactId>
		</dependency>
		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-databind</artifactId>
		</dependency>
		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-annotations</artifactId>
		</dependency>
		<dependency>
			<groupId>org.testng</groupId>
			<artifactId>testng</artifactId>
			<version>RELEASE</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>net.sourceforge.tess4j</groupId>
			<artifactId>tess4j</artifactId>
			<version>${tess4j.version}</version>
		</dependency>
		<dependency>
			<groupId>org.apache.tika</groupId>
			<artifactId>tika-parsers</artifactId>
			<version>1.17</version>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<!-- JWT -->
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>${jwt.version}</version>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>${jwt.version}</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>${jwt.version}</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-cache</artifactId>
		</dependency>
		<dependency>
			<groupId>com.github.ben-manes.caffeine</groupId>
			<artifactId>caffeine</artifactId>
			<version>3.2.2</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.14.0</version>
				<configuration>
					<release>21</release>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
				</configuration>
			</plugin>
			<plugin>
				<groupId>com.github.eirslett</groupId>
				<artifactId>frontend-maven-plugin</artifactId>
				<version>${frontend-maven-plugin.version}</version>
				<configuration>
					<nodeVersion>${node.version}</nodeVersion>
					<workingDirectory>${frontend-src-dir}</workingDirectory>
					<installDirectory>${project.build.directory}</installDirectory>
				</configuration>
				<executions>
					<execution>
						<id>install-frontend-tools</id>
						<goals>
							<goal>install-node-and-npm</goal>
						</goals>
					</execution>
					<execution>
						<id>npm-install</id>
						<goals>
							<goal>npm</goal>
						</goals>
						<configuration>
							<arguments>install</arguments>
						</configuration>
					</execution>
					<execution>
						<id>build-frontend</id>
						<goals>
							<goal>npm</goal>
						</goals>
						<phase>prepare-package</phase>
						<configuration>
							<arguments>run build</arguments>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<artifactId>maven-resources-plugin</artifactId>
				<version>3.0.1</version>
				<executions>
					<execution>
						<id>position-react-build</id>
						<goals>
							<goal>copy-resources</goal>
						</goals>
						<phase>prepare-package</phase>
						<configuration>
							<outputDirectory>${project.build.outputDirectory}/static</outputDirectory>
							<resources>
								<resource>
									<directory>${frontend-src-dir}/build</directory>
									<filtering>false</filtering>
								</resource>
							</resources>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>

			<!-- Patch tess4j jar with Linux native libs at build time (reproducible, no ~/.m2 editing) -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<version>3.6.1</version>
				<executions>
					<execution>
						<id>copy-tess4j-jar-for-patching</id>
						<phase>generate-resources</phase>
						<goals>
							<goal>copy</goal>
						</goals>
						<configuration>
							<artifactItems>
								<artifactItem>
									<groupId>net.sourceforge.tess4j</groupId>
									<artifactId>tess4j</artifactId>
									<version>${tess4j.version}</version>
									<outputDirectory>${project.build.directory}/patched-deps</outputDirectory>
									<destFileName>tess4j-${tess4j.version}.jar</destFileName>
								</artifactItem>
							</artifactItems>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>exec-maven-plugin</artifactId>
				<version>3.3.0</version>
				<executions>
					<execution>
						<id>patch-tess4j-jar-linux-x86-64</id>
						<phase>generate-resources</phase>
						<goals>
							<goal>exec</goal>
						</goals>
						<configuration>
							<executable>bash</executable>
							<arguments>
								<argument>-lc</argument>
								<argument>
									<![CDATA[
set -euo pipefail
JAR="${tess4j.patched.jar}"
NATIVE_SRC="${tess4j.native.dir}"
WORK_DIR="${project.build.directory}/tess4j-native"
NATIVE_DIR_NAME="linux-x86-64"

# Skip patching if no native libs are provided
if ! ls "${NATIVE_SRC}"/libtesseract.so* >/dev/null 2>&1; then
  echo "No ${NATIVE_SRC}/libtesseract.so* found; skipping tess4j jar patch step."
  exit 0
fi
if ! ls "${NATIVE_SRC}"/liblept.so* >/dev/null 2>&1; then
  echo "No ${NATIVE_SRC}/liblept.so* found; skipping tess4j jar patch step."
  exit 0
fi

mkdir -p "${WORK_DIR}/${NATIVE_DIR_NAME}"
rm -f "${WORK_DIR:?}/${NATIVE_DIR_NAME}"/libtesseract.so* "${WORK_DIR:?}/${NATIVE_DIR_NAME}"/liblept.so* || true

cp -a "${NATIVE_SRC}"/libtesseract.so* "${WORK_DIR}/${NATIVE_DIR_NAME}/"
cp -a "${NATIVE_SRC}"/liblept.so* "${WORK_DIR}/${NATIVE_DIR_NAME}/"

# Ensure the directory is added/updated in the jar
cd "${WORK_DIR}"
jar uf "${JAR}" "${NATIVE_DIR_NAME}"

echo "Patched ${JAR} with native libs under ${NATIVE_DIR_NAME}/"
									]]>
								</argument>
							</arguments>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<!-- Make Spring Boot use the patched tess4j jar if it exists (so it wins on the classpath) -->
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
					<!-- Include the patched jar into the repackaged boot jar -->
					<includeSystemScope>true</includeSystemScope>
				</configuration>
			</plugin>

			<!-- Add the patched tess4j jar as a system-scoped dependency for repackaging -->
		</plugins>
	</build>

	<!-- System dependency pointing at the patched jar produced during the build.
	     This is only used when patching is enabled (libs present). -->
	<profiles>
		<profile>
			<id>use-patched-tess4j</id>
			<activation>
				<file>
					<exists>${tess4j.native.dir}/libtesseract.so</exists>
				</file>
			</activation>
			<dependencies>
				<dependency>
					<groupId>net.sourceforge.tess4j</groupId>
					<artifactId>tess4j-patched</artifactId>
					<version>${tess4j.version}</version>
					<scope>system</scope>
					<systemPath>${tess4j.patched.jar}</systemPath>
				</dependency>
			</dependencies>
		</profile>
	</profiles>
</project>
